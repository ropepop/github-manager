# GitHub Manager

Agent-guided discovery, sanitization, publishing, and syncing of local coding projects to GitHub.

## What It Does

- Agent-guided discovery, sanitization, publishing, and syncing of local coding projects to GitHub.
- Organizes the main public surface around github_manager/, tests/, .gitignore, pyproject.toml.
- Includes 24 public project file(s) in the sanitized copy.

## Highlights

- Repository structure is summarized so visitors can quickly understand what is included.
- Setup commands are inferred from the sanitized manifests and scripts.
- Verification commands are listed for quick local checks.
- Primary detected language signals: Python.
- Previous public README content is available in README.previous.md.

## Tech Stack

- Python package (>=3.11).

## Quick Start

1. Install the package in editable mode.

```bash
python3 -m pip install -e .
```

2. Run the `github-manager` command help.

```bash
github-manager --help
```

## Project Map

- `github_manager/`: Core Python package for the manager.
- `tests/`: Automated tests.
- `.gitignore`: Ignore rules for generated and local-only files.
- `pyproject.toml`: Python package manifest and package metadata.

## Testing

1. Run Python tests.

```bash
python3 -m unittest discover -q
```

<!-- github-manager-readme:start -->
## Public Copy Notes

- This README was generated by GitHub Manager for a sanitized public copy.
- Files that look private, generated, or unsafe for public release are left out before each update.
- Risky files removed in the latest sanitation pass: 0
- The previous sanitized README is preserved in `README.previous.md`.
<!-- github-manager-readme:end -->
